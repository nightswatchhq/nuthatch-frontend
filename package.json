{
  "name": "nuthatch-frontend",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Website for Nuthatch - the self-hosted, AI-native blockchain indexer.",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run lint:docs && node scripts/generate-sitemap.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint:docs": "node scripts/lint-docs.mjs"
  },
  "dependencies": {
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  }
}
